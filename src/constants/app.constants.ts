export const SWAGGER_PATH = 'docs';

// Filename at the project root and the URL path it's served from — kept
// distinct since one's a filesystem lookup and the other's a route/link.
export const FAVICON_FILE = 'favicon.ico';
export const FAVICON_URL_PATH = '/favicon.ico';
